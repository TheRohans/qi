#include <stdio.h>
/**
 * This is a mulitline comment
 */
void main(void) {
  int x = 1;
  char *thing;

  void *thing = malloc(sizeof(char) * 10);
  &thing = "0123456789";

  if (x == 3) {
    // then do stuff with things.
    printf("%s ", *stuff);
  }

  printf("%s", *thing);
}
